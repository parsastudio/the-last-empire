import { Coordinate } from "@/domain/map/coordinate.schema";

export class AiGridCampaignGenerator {
  public generateCampaignTargets(): Map<string, Coordinate> {
    return new Map<string, Coordinate>();
  }
}
